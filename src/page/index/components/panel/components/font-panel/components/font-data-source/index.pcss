.init {
    margin-top: 22px;

    .row {
        display: flex;
        flex-direction: column;

        .rowTitle {
            display: flex;
            justify-content: center;
            font-size: 14px;
            font-weight: 700;
        }

        .content {
            margin-top: 12px;
            height: 200px;
        }
    }
}